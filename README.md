# Sparks - Сервис подбора подарков с ИИ

Веб-приложение для подбора подарков с использованием Google Gemini API.

## 📖 Документация

- **[Полное руководство по настройке](SETUP_GUIDE.md)** - пошаговая инструкция по установке и настройке проекта
- **[Настройка Gemini API](GEMINI_API_SETUP.md)** - детальная информация о получении и настройке API ключа

## Структура проекта

Проект использует единый веб-сервер с интегрированным Gemini API:

- **Веб-сервер** (`server.js`) - раздает статические файлы (HTML, CSS, JS) и обрабатывает запросы к Google Gemini API
   - Порт: `3000`
   - Запуск: `npm start` или `node server.js`
   - API эндпоинты:
     - `POST /api/gemini` - запросы к Gemini API
     - `GET /api/config` - получение конфигурации
     - `GET /api/test` - тест API
     - `GET /api-status` - статус сервера

## Установка

```bash
npm install
```

## Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
   Или создайте файл `.env` вручную.

2. Откройте файл `.env` и заполните ваши данные:
   ```env
   GEMINI_API_KEY=ваш_api_ключ_здесь
   GEMINI_MODEL=gemini-pro
   ```

   **Важно:** Файл `.env` уже добавлен в `.gitignore` и не будет попадать в git.

3. Получить API ключ можно в [Google AI Studio](https://aistudio.google.com/):
   - Зарегистрируйтесь или войдите в аккаунт
   - Нажмите "Get API Key" в левом меню
   - Создайте новый ключ и скопируйте его
   - **Важно:** Ключ должен начинаться с `AIza` (это ключ от Gemini, а не от других сервисов)

4. Модель по умолчанию: `gemini-pro`. Можно использовать другие модели:
   - `gemini-pro` - стандартная модель
   - `gemini-pro-vision` - модель с поддержкой изображений
   - `gemini-1.5-pro` - более новая версия (если доступна)

## Запуск

Запустите один сервер, который обрабатывает и веб-страницы, и API запросы:

```bash
npm start
# или
node server.js
```

### Запуск в фоновом режиме (Windows PowerShell)

```powershell
Start-Process node -ArgumentList "server.js" -WindowStyle Normal
```

## Доступ к приложению

- **Главная страница:** http://localhost:3000
- **Статус сервера:** http://localhost:3000/api-status
- **Тест API:** http://localhost:3000/api/test

## Настройка Gemini API

Настройки API хранятся в файле `.env` (см. раздел "Настройка переменных окружения" выше).

Если возникли проблемы, см. файл `GEMINI_API_SETUP.md`.

## Структура файлов

```
project-ai-api/
├── server.js          # Веб-сервер с интегрированным Gemini API (порт 3000)
├── index.html         # Главная страница
├── ai-search.html     # Страница с ИИ чатом
├── css/               # Стили
├── js/                # JavaScript файлы
└── images/            # Изображения
```

## Разработка

Для разработки с автоматической перезагрузкой:

```bash
npm run dev
```

*Требуется установленный `nodemon`: `npm install -g nodemon`*
