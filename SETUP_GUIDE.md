# 📖 Полное руководство по настройке проекта Sparks

## 📋 Содержание

1. [Описание проекта](#описание-проекта)
2. [Системные требования](#системные-требования)
3. [Установка](#установка)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Получение API ключа Gemini](#получение-api-ключа-gemini)
6. [Запуск проекта](#запуск-проекта)
7. [Проверка работы](#проверка-работы)
8. [Структура проекта](#структура-проекта)
9. [Решение проблем](#решение-проблем)
10. [Дополнительные настройки](#дополнительные-настройки)

---

## 🎯 Описание проекта

**Sparks** - веб-приложение для подбора подарков с использованием искусственного интеллекта (Google Gemini API). Проект использует единый веб-сервер:

- **Веб-сервер** (`server.js`) - раздает статические файлы (HTML, CSS, JS) и обрабатывает запросы к Google Gemini API на порту 3000

### Основные возможности:

- 💬 Чат с ИИ-помощником для подбора подарков
- 🎁 База данных подарков с фильтрацией
- 🔍 Умный поиск на основе предпочтений пользователя
- 💾 Сохранение истории чата в браузере
- 🎨 Современный и адаптивный интерфейс

---

## 💻 Системные требования

### Обязательные требования:

- **Node.js** версии 18.0.0 или выше (рекомендуется 20.x или 22.x)
- **npm** (обычно устанавливается вместе с Node.js)
- **Google аккаунт** для получения API ключа Gemini
- **Браузер** с поддержкой современного JavaScript (Chrome, Firefox, Edge, Safari)

### Проверка версии Node.js:

```bash
node --version
```

Если версия ниже 18.0.0, обновите Node.js с [официального сайта](https://nodejs.org/).

### Проверка версии npm:

```bash
npm --version
```

---

## 📦 Установка

### Шаг 1: Клонирование или скачивание проекта

Если проект находится в репозитории:
```bash
git clone <url-репозитория>
cd project-ai-api
```

Или просто откройте папку проекта в терминале.

### Шаг 2: Установка зависимостей

Откройте терминал в корневой папке проекта и выполните:

```bash
npm install
```

Эта команда установит все необходимые пакеты:
- `express` - веб-сервер
- `cors` - обработка CORS запросов
- `dotenv` - загрузка переменных окружения

### Шаг 3: Проверка установки

Убедитесь, что папка `node_modules` создана и содержит установленные пакеты.

---

## ⚙️ Настройка переменных окружения

### Шаг 1: Создание файла .env

Создайте файл `.env` в корневой папке проекта. Вы можете:

**Вариант A:** Создать файл вручную
- Создайте новый файл с именем `.env` (без расширения)
- Убедитесь, что файл находится в корне проекта (рядом с `package.json`)

**Вариант B:** Использовать терминал
```bash
# Windows (PowerShell)
New-Item -Path .env -ItemType File

# Windows (CMD)
type nul > .env

# Linux/Mac
touch .env
```

### Шаг 2: Заполнение файла .env

Откройте файл `.env` в текстовом редакторе и добавьте следующие строки:

```env
GEMINI_API_KEY=ваш_api_ключ_здесь
GEMINI_MODEL=gemini-pro
```

**Важно:**
- Не используйте кавычки вокруг значений
- Не добавляйте пробелы вокруг знака `=`
- Каждая переменная на отдельной строке
- Файл `.env` уже добавлен в `.gitignore` и не будет попадать в git

---

## 🔑 Получение API ключа Gemini

### Пошаговая инструкция:

1. **Откройте Google AI Studio**
   - Перейдите по адресу: https://aistudio.google.com/
   - Войдите в свой Google аккаунт

2. **Получите API ключ**
   - В левом меню нажмите на кнопку **"Get API Key"** (Получить API ключ)
   - Если кнопки нет, нажмите на иконку ключа в верхней панели

3. **Создайте или выберите проект**
   - Выберите существующий проект Google Cloud или создайте новый
   - Нажмите "Create API Key" (Создать API ключ)

4. **Скопируйте ключ**
   - Скопируйте созданный API ключ
   - **Важно:** Ключ от Gemini начинается с `AIza` и имеет длину около 39 символов
   - Если ключ начинается с `AQVN` или другой последовательности - это ключ от другого сервиса

5. **Вставьте ключ в .env**
   - Откройте файл `.env`
   - Вставьте ключ после `GEMINI_API_KEY=`
   - Сохраните файл

### Пример правильного ключа:

```env
GEMINI_API_KEY=AIzaSyCky2XxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXx
```

### Выбор модели Gemini:

В файле `.env` вы можете указать одну из доступных моделей:

- **`gemini-pro`** - стандартная модель (рекомендуется для начала)
- **`gemini-1.5-pro`** - более новая версия с расширенным контекстом
- **`gemini-1.5-flash`** - быстрая модель для простых задач
- **`gemini-2.0-flash-lite`** - облегченная версия (если доступна)

**Пример:**
```env
GEMINI_MODEL=gemini-pro
```

---

## 🚀 Запуск проекта

Проект использует **единый веб-сервер**, который обрабатывает и веб-страницы, и API запросы к Gemini.

### Запуск сервера

Откройте терминал в корневой папке проекта и выполните:

```bash
npm start
# или
node server.js
```

Вы должны увидеть:
```
==================================================
🚀 SPARKS - ВЕБ-СЕРВЕР С ИНТЕГРИРОВАННЫМ GEMINI API
==================================================
📄 Главная страница: http://localhost:3000
📁 Статические файлы доступны на порту 3000
🔗 API для ИИ: http://localhost:3000/api/gemini
⚙️  Конфигурация: http://localhost:3000/api/config
🧪 Тест API: http://localhost:3000/api/test
📊 Статус: http://localhost:3000/api-status
🔑 API Key: AIzaSyCky2...
🤖 Модель: gemini-pro
==================================================
✅ Все готово! Откройте http://localhost:3000 в браузере
==================================================
```

### Запуск в фоновом режиме (Windows PowerShell)

```powershell
Start-Process node -ArgumentList "server.js" -WindowStyle Normal
```

### Запуск для разработки

Если установлен `nodemon` (автоматическая перезагрузка при изменениях):

```bash
npm run dev
```

**Установка nodemon:**
```bash
npm install -g nodemon
```

---

## ✅ Проверка работы

### 1. Проверка веб-сервера

Откройте в браузере:
```
http://localhost:3000
```

Вы должны увидеть главную страницу сайта Sparks.

### 2. Проверка статуса сервера

Откройте в браузере:
```
http://localhost:3000/api-status
```

Вы должны увидеть JSON-ответ со статусом сервера и конфигурацией Gemini API.

### 3. Тест API Gemini

Откройте в браузере:
```
http://localhost:3000/api/test
```

Если все настроено правильно, вы увидите JSON-ответ от Gemini API с сообщением "Работает" или похожим.

### 4. Проверка ИИ-чата

1. Откройте http://localhost:3000
2. Перейдите на страницу "ИИ Поиск" (или откройте http://localhost:3000/ai-search.html)
3. Начните диалог с ИИ-помощником
4. Если ИИ отвечает на ваши сообщения - все работает правильно!

---

## 📁 Структура проекта

```
project-ai-api/
├── server.js              # Веб-сервер с интегрированным Gemini API (порт 3000)
├── package.json           # Зависимости и скрипты
├── package-lock.json      # Зафиксированные версии пакетов
├── .env                   # Переменные окружения (создается вручную)
├── .gitignore            # Игнорируемые файлы для git
│
├── index.html            # Главная страница
├── ai-search.html        # Страница с ИИ-чатом
├── login.html            # Страница входа
├── register.html         # Страница регистрации
├── profile.html          # Страница профиля
├── recommendations.html  # Страница рекомендаций
├── secret-box.html       # Страница секретного подарка
│
├── css/
│   └── style.css         # Основные стили
│
├── js/
│   ├── main.js           # Основная логика
│   ├── ai-search.js      # Логика ИИ-чата
│   ├── login.js          # Логика входа
│   ├── register.js       # Логика регистрации
│   ├── profile.js        # Логика профиля
│   └── ...
│
├── images/               # Изображения
│
├── node_modules/         # Установленные пакеты (создается после npm install)
│
├── README.md             # Основная документация
├── SETUP_GUIDE.md        # Это руководство
└── GEMINI_API_SETUP.md   # Детальная настройка Gemini API
```

---

## 🔧 Решение проблем

### Проблема 1: "Cannot find module 'dotenv'"

**Причина:** Не установлены зависимости.

**Решение:**
```bash
npm install
```

### Проблема 2: "Port 3000 is already in use"

**Причина:** Порт 3000 уже занят другим процессом.

**Решение:**
- Закройте другое приложение, использующее порт 3000
- Или измените порт в `server.js` (строка с `const PORT = 3000;`)

### Проблема 3: "Port 3000 is already in use" (дополнительно)

**Причина:** Порт 3000 уже занят другим процессом.

**Решение:**
- Закройте другое приложение, использующее порт 3000
- Или измените порт в `server.js` (строка с `const PORT = 3000;`)
- Также обновите `APP_CONFIG.API_URL` и `APP_CONFIG.CONFIG_URL` в `js/ai-search.js`

### Проблема 4: "ERR_CONNECTION_REFUSED"

**Причина:** Сервер не запущен.

**Решение:**
1. Убедитесь, что сервер запущен (`npm start` или `node server.js`)
2. Проверьте, что он работает на порту 3000
3. Откройте http://localhost:3000 в браузере для проверки
4. Проверьте статус API: http://localhost:3000/api-status

### Проблема 5: "API key not valid" (400 Bad Request)

**Причина:** Неверный или отсутствующий API ключ.

**Решение:**
1. Проверьте файл `.env` - ключ должен быть указан
2. Убедитесь, что ключ начинается с `AIza` (это ключ от Gemini)
3. Проверьте, что нет лишних пробелов или кавычек
4. Получите новый ключ в [Google AI Studio](https://aistudio.google.com/)

### Проблема 6: "401 Unauthorized"

**Причина:** API ключ недействителен или истек.

**Решение:**
1. Проверьте ключ в Google AI Studio
2. Создайте новый ключ, если старый не работает
3. Обновите ключ в файле `.env`
4. Перезапустите прокси-сервер

### Проблема 7: "403 Forbidden"

**Причина:** Нет прав доступа к API.

**Решение:**
1. Проверьте права доступа в Google Cloud Console
2. Убедитесь, что Gemini API включен для вашего проекта
3. Проверьте настройки проекта в Google AI Studio

### Проблема 8: "429 Too Many Requests"

**Причина:** Превышен лимит запросов.

**Решение:**
1. Подождите несколько минут
2. Проверьте лимиты в Google AI Studio
3. Рассмотрите возможность перехода на платный тариф

### Проблема 9: ИИ не отвечает в чате

**Причина:** Проблема с подключением к серверу или API.

**Решение:**
1. Убедитесь, что сервер запущен (`npm start`)
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Проверьте, что сервер доступен: http://localhost:3000
4. Проверьте статус API: http://localhost:3000/api-status
5. Проверьте тест API: http://localhost:3000/api/test
6. Убедитесь, что файл `.env` содержит правильный `GEMINI_API_KEY`

### Проблема 10: История чата не сохраняется

**Причина:** Проблемы с localStorage в браузере.

**Решение:**
1. Убедитесь, что в браузере включены cookies и localStorage
2. Проверьте настройки приватности браузера
3. Попробуйте использовать другой браузер

---

## 🎨 Дополнительные настройки

### Изменение портов

Если порты 3000 или 3001 заняты, вы можете изменить их:

**Веб-сервер (`server.js`):**
```javascript
const PORT = 3000; // Измените на нужный порт
```

**Прокси-сервер (`proxy.js`):**
```javascript
const PORT = 3001; // Измените на нужный порт
```

**В клиентском коде (`js/ai-search.js`):**
```javascript
const APP_CONFIG = {
    API_URL: 'http://localhost:3000/api/gemini', // Обновите порт
    CONFIG_URL: 'http://localhost:3000/api/config', // Обновите порт
    // ...
};
```

### Настройка CORS

Если нужно разрешить запросы с других доменов, откройте `server.js` и измените настройки CORS:

```javascript
app.use(cors({
    origin: '*', // Или укажите конкретные домены
    methods: ['GET', 'POST', 'OPTIONS'],
    allowedHeaders: ['Content-Type', 'Authorization'],
    credentials: true
}));
```

### Изменение параметров генерации

В файле `server.js` можно изменить параметры генерации ответов от Gemini (в функции `convertToGeminiFormat`):

```javascript
generationConfig: {
    temperature: 0.7,      // Креативность (0.0-1.0)
    maxOutputTokens: 1000,  // Максимальная длина ответа
    topP: 0.8,              // Разнообразие ответов
    topK: 40                // Количество вариантов
}
```

### Добавление новых подарков

Откройте файл `js/ai-search.js` и найдите массив `giftsDatabase`. Добавьте новые объекты подарков в формате:

```javascript
{
    id: уникальный_номер,
    name: "Название подарка",
    price: "Цена",
    description: "Описание",
    category: "Категория",
    tags: ["тег1", "тег2"],
    age: ["13-18", "19-30"],
    budget: ["1000-3000", "3000-5000"],
    interests: ["Спорт", "Технологии"],
    occasion: ["День рождения", "Новый год"]
}
```

---

## 📚 Полезные ссылки

- [Документация Gemini API](https://ai.google.dev/docs)
- [Google AI Studio](https://aistudio.google.com/)
- [Node.js документация](https://nodejs.org/docs/)
- [Express.js документация](https://expressjs.com/)
- [Документация dotenv](https://github.com/motdotla/dotenv)

---

## 🆘 Получение помощи

Если у вас возникли проблемы, которые не описаны в этом руководстве:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи серверов в терминале
3. Убедитесь, что все шаги установки выполнены правильно
4. Проверьте файл `GEMINI_API_SETUP.md` для детальной информации о настройке API

---

## ✅ Чек-лист настройки

Используйте этот чек-лист для проверки правильности настройки:

- [ ] Node.js версии 18+ установлен
- [ ] npm установлен
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` создан
- [ ] API ключ Gemini получен и добавлен в `.env`
- [ ] Модель Gemini указана в `.env`
- [ ] Сервер запущен (`npm start`)
- [ ] Главная страница открывается (http://localhost:3000)
- [ ] Статус сервера работает (http://localhost:3000/api-status)
- [ ] Тест API работает (http://localhost:3000/api/test)
- [ ] ИИ-чат отвечает на сообщения

---

**Удачной настройки! 🎉**

